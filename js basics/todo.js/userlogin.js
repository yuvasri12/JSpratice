let email = Yuvasri
let pass = '12345';
function emailCheck (){
console.log(email.toLocaleCase());
console.log(email.trim());// reudce space
console.log(email.include('gmail'));// check word
if(user.include('@')){
    return true;
} else{
    return false;
}
}
emailCheck(email);